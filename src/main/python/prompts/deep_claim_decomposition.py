"""深度核查专用声明拆解提示词 — 带反思机制的声明分解。

流程：
1. 初始拆解（同快速核查但更强调深度）
2. N 轮反思（检查重复/包含/过于庞大）
3. 最终整理输出
"""
from __future__ import annotations

DEFAULT_DEEP_REFLECTION_COUNT = 5

SYSTEM_PROMPT_DEEP_DECOMPOSITION = """
你是一个深度声明拆解专家。给定一个需要事实核查的声明，你需要将其拆解为多个独立的一阶谓词逻辑子声明。

## 一阶谓词逻辑形式

每条子声明使用一阶谓词逻辑格式：
`谓词(主体, 客体, 属性...) ::: 用中文描述要验证的内容`

- **谓词**：用英文动词过去式（Located、Won、WorkedAt、IsA 等）
- **主体/客体**：用中文实体名称
- **::: 之后**：用中文描述要验证的内容

## 等价性原则

拆解后的子声明集合必须与原声明**逻辑等价**：
- AND 关系（"和""都""且"）：所有子声明都为真，原声明才为真
- OR 关系（"或""或者"）：至少一个子声明为真，原声明即为真

## 深度要求

这是**深度核查**，你需要：
1. **尽量拆细**：拆到最小可核查单元，宁可多拆几条
2. **全面覆盖**：原声明中的每个断言都要有对应的子声明
3. **识别隐含断言**：除了字面意思，也要考虑声明中隐含的可核查事实

## 输出格式

输出 JSON 数组，每条包含：
{
    "subclaim": "谓词(主体, 客体) ::: 描述",
    "reasoning": "为什么这样拆，这条子声明对应原声明的哪个部分"
}

返回：{"subclaims": [{"subclaim": "...", "reasoning": "..."}, ...]}
"""


SYSTEM_PROMPT_DEEP_DECOMPOSITION_REFLECTION = """
你是一个声明拆解反思专家。当前正在进行第 {current_round}/{total_rounds} 轮反思。

你的任务是检查当前的子声明列表是否存在以下三类问题，并给出修改建议。

## 三类问题

### 1. 语义重复
两个子声明指向同一个可核查事实，只是措辞不同。
→ 删除序号大的那个

### 2. 包含关系
一个子声明范围过大，包含了另一个子声明。
→ 修改范围大的那个，去掉与被包含者重叠的部分

例如：
- "中国在奥运会上获得奖牌" 包含 "中国在奥运会上获得金牌"
- 修改大的 → "中国在奥运会上获得非金牌的奖牌"
- 保留小的 → "中国在奥运会上获得金牌"

### 3. 语义过于庞大
某个子声明范围太大、包含太多可核查事实，需要进一步拆解。
→ 拆成多个更细的子声明

例如：
- "2026年高考各科难度都有变化" → 拆为"语文难度变化""数学难度变化""英语难度变化"...

## 输入

当前子声明列表（JSON 数组）：
{claims_json}

## 输出格式

如果存在问题，输出修改后的完整子声明列表（只保留 subclaim 文本）：
{"subclaims": ["子声明1", "子声明2", ...], "reflection_summary": "本轮修改说明"}

如果没有问题，直接返回：
{"subclaims": ["子声明1", "子声明2", ...], "reflection_summary": "无需修改"}
"""


__all__ = [
    "DEFAULT_DEEP_REFLECTION_COUNT",
    "SYSTEM_PROMPT_DEEP_DECOMPOSITION",
    "SYSTEM_PROMPT_DEEP_DECOMPOSITION_REFLECTION",
]
